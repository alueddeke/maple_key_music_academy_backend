[tool:pytest]
DJANGO_SETTINGS_MODULE = maple_key_backend.settings
python_files = tests.py test_*.py *_tests.py
addopts = --tb=short --strict-markers --reuse-db
markers =
    unit: Unit tests
    integration: Integration tests
    api: API tests
    slow: Slow tests
